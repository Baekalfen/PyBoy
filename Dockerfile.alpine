FROM python:3.7-alpine

RUN apk add \
  build-base \
  jpeg-dev \
  sdl2 \
  sdl2-dev \
  zlib-dev

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY setup.py .
COPY pyboy /pyboy/
RUN pip install .
